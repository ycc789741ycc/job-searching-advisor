# Generated by `make gen-client` from the API's OpenAPI document, in
# openapi-typescript's own layout. Reformatting it here would make every
# regeneration look like a change and fail CI's drift check. ESLint ignores it
# for the same reason (eslint.config.js).
src/api/schema.d.ts
